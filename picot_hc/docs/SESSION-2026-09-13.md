# Sessie 2026-09-13

## Doel en wijzigingscontract

De afzonderlijke installatieroute was volgens de gebruiker eerder niet zichtbaar
in HA. De verantwoordelijke grens is appdistributie: HC toevoegen naast Energy
Devices in de bestaande repository. HC-ADR-006 en HC-ADR-011 zijn leidend.
Het HEMS Canonical Pipeline Contract blijft ongewijzigd; HC neemt geen deel aan
die pipeline. HEMS-planning, adapters, versies en Energy Devices vallen buiten
scope. Invariant: HC start met alleen HA en eigen configuratie/opslag.
Verificatie: geïsoleerde HC-tests, manifestlocatie, unieke slug, dashboard-syntaxis
en container-build in CI. Terugdraaigrens: uitsluitend deze toevoeging verwijderen;
bestaande HEMS-code en gegevens worden niet gemigreerd.

## Besluiten en implementatie

- HC is een afzonderlijke app in `picot_hc/`, met eigen container en versie.
- De eerste versie leest HA en bewaart metingen; geen opdrachten of planner.
- De HA-winkel kan de app vanaf de gedeelde repository aanbieden na merge.
- CI bouwt de HC-container rechtstreeks uit de PR-inhoud.
- De bestaande HEMS- en Energy Devices-containers gebruiken geen HC-code.

## Open punten

- CI-resultaten en live HA-installatie afzonderlijk controleren.
- Ecowitt-entiteiten, prijsattributen en eenheden live vaststellen.
- Visuele controle in de echte HA-Ingress-interface.
- Bronbediening, badkamerknop en prijsplanner zijn vervolgwerk.

## Volgende sessie

Na merge de winkel vernieuwen, HC installeren en metingen/prijsgrafiek controleren.
Daarna Ecowitt koppelen en bronbediening met terugmeldingen voorbereiden.

## Vervolg — temperatuurinstellingen op dashboard

Gebruiker bevestigt dat HC live staat. Probleem: temperatuurgrenzen wijzigen via
de appconfiguratie is onhandig en onoverzichtelijk. Eigenaar: HC-configuratie en
dashboard, volgens HC-ADR-006 en het configuratieonderdeel van het HC-ontwerp.
Minimum, gewenst en maximum komen per zone op het dashboard. Alleen deze drie
velden zijn beschrijfbaar; apparaten, HEMS, vochtregeling en planning zijn buiten
scope. Invariant: geen HA-opdrachten, validatie vóór atomaire opslag, geen wijziging
aan andere zones, opslag overleeft herstart. Dashboardwaarden hebben per zone
voorrang op appopties; leeg betekent niet ingesteld. Een extra SQLite-tabel
bewaart de instellingen zonder bestaande meetgeschiedenis te wijzigen.

Verificatie omvat HTTP-opslaan, herstart, ongeldige waarden, toegangsbescherming,
opslagfouten en behoud van invoer tijdens dashboardverversing. Terugrollen naar
dev.1 laat de extra tabel staan maar gebruikt weer de appopties voor temperaturen.
De gewone meetgeschiedenis blijft leesbaar. Live controle van dev.2 volgt na update.

## Vervolg — Buienradar op dashboard

Gebruiker vraagt weer en een mogelijke latere reactie op de forecast. Entiteit en
eenheden zijn aangeleverd: weather.buienradar, °C, km/h, mm, hPa, supported_features 1.
HC-configuratie, HA-inleeslaag, meetopslag en dashboard zijn verantwoordelijk.
HC-ADR-004 (woninggedrag), HC-ADR-006 (zelfstandigheid) en HC-ADR-010 (meetkwaliteit)
blijven leidend. Nieuwe module leest huidige toestand en vraagt dagverwachtingen
op via de vaste uitleesactie weather.get_forecasts. Geen apparaatopdrachten,
geen HEMS-wijziging, geen gebruik als vervangende eigen buitensensor of planner.

Invarianten: bron-/ontvangsttijd en eenheden zichtbaar, onbekend is geen nul,
fouten in forecasts blokkeren overige metingen niet. Cache blijft na herstart
beschikbaar; opslag in bestaande meetsnapshots. Terugrollen naar dev.2 negeert
het extra weeronderdeel zonder temperatuurinstellingen of geschiedenis te wissen.
Vervolg: live forecast controleren, daarna dag-/uurresolutie en weersinvloed in
een afzonderlijke plannerwijziging uitwerken.
